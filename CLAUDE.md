# Gacha Map 프로젝트 CLAUDE.md

## 프로젝트 개요
- 이 프로젝트는 가챠샵 위치와 관련 정보를 보여주는 서비스입니다.
- 사용자가 주변 가챠샵을 찾고, 이후에는 특정 가챠가 있는 샵도 찾을 수 있게 만드는 것이 목표입니다.

## 기술 기준
- 기본 언어는 TypeScript를 사용합니다.
- 명확한 이유 없이 새로운 프레임워크나 라이브러리를 추가하지 않습니다.
- 웹앱 프로젝트라면 기존 코드베이스에 다른 기준이 없을 경우 Next.js를 기준으로 작업합니다.
- 전역 상태 관리는 Redux를 사용합니다.
- 데이터베이스 관련 작업은 기존 Supabase/Postgres 구조를 우선 따릅니다.

## 구조 및 아키텍처 규칙
- Atomic Design 패턴을 사용합니다.
- MVVM 패턴을 사용합니다.
- 화면(UI), 상태, 비즈니스 로직의 역할을 분리합니다.
- 라우트 단위 파일은 `app/`에 둡니다.
- 공통 UI 컴포넌트는 `components/`에 둡니다.
- 공통 로직과 외부 연동 코드는 `lib/`에 둡니다.
- 공통 타입은 `types/`에 둡니다.
- 프로젝트 기준 문서는 `docs/`에 둡니다.
- 명확한 이유가 없으면 새로운 최상위 디렉토리를 만들지 않습니다.

## 컴포넌트 규칙
- 재사용 가능성이 높은 컴포넌트는 분리해서 재사용합니다.
- 공통 컴포넌트와 도메인 특화 컴포넌트의 역할을 구분합니다.
- 컴포넌트는 하나의 책임에 집중하게 유지합니다.
- 다만 MVP 단계에서는 과도한 분리와 불필요한 추상화는 피합니다.

## 스타일링 규칙
- 스타일링은 styled-components를 사용합니다.
- 다른 스타일링 방식을 혼용하지 않습니다.
- 색상 값은 공통 `color.ts` 파일에 정리하고 import 해서 사용합니다.
- 색상값을 컴포넌트 내부에 직접 하드코딩하지 않습니다.
- 반복되는 스타일 패턴은 공통 스타일 또는 재사용 가능한 스타일 컴포넌트로 분리합니다.

## 상수 및 타입 규칙
- enum 값은 개별 파일로 분리해서 관리합니다.
- constant 값도 개별 파일로 분리해서 관리합니다.
- 매직 넘버, 문자열 하드코딩을 최소화합니다.
- 재사용 가능한 값은 상수 파일 또는 enum 파일에서 import 해서 사용합니다.

## 상태관리 규칙
- 여러 화면에서 공유되거나 흐름상 전역 관리가 필요한 값은 Redux로 관리합니다.
- 지역 상태로 충분한 값은 전역 상태로 올리지 않습니다.
- 전역 상태와 UI 전용 상태를 구분해서 관리합니다.

## 다국어 규칙
- 사용자에게 보여지는 텍스트는 가능한 한 i18n으로 관리합니다.
- 문자열을 컴포넌트 내부에 직접 하드코딩하지 않는 방향을 우선합니다.
- 지원 언어는 한국어, 영어, 일본어, 중국어입니다.
- 기본 언어는 한국어입니다.
- 버튼 문구, 안내 문구, 에러 문구, 공통 텍스트는 i18n 리소스로 분리합니다.

## 데이터 및 DB 규칙
- 데이터베이스 스키마와 수집 데이터는 신중하게 다룹니다.
- 스키마 관련 작업 전에는 `docs/db-schema.md`와 관련 문서를 먼저 확인합니다.
- 스키마 변경, 마이그레이션, 파괴적 쿼리는 먼저 설명한 뒤 진행합니다.
- 수집 원본 데이터와 정규화된 데이터는 분리해서 다룹니다.
- 필드 의미나 데이터 계약을 임의로 변경하지 않습니다.

## 도메인 규칙
- 샵 위치 데이터와 가챠 아이템 데이터는 별개의 관심사로 다룹니다.
- 위치 데이터는 수집 데이터 또는 관리자 검증 데이터를 기준으로 다룹니다.
- 가챠 아이템 보유 여부는 변동성이 크므로 영구적으로 정확한 정보처럼 가정하지 않습니다.
- 지도, 샵 상세, 검색, 제보 등 실제 사용자 흐름을 우선합니다.
- 기본적으로 한국 사용자 기준 UX를 우선합니다.

## 문서 규칙
- 구조적 결정을 하기 전에는 관련 문서를 먼저 확인합니다.
- 구조, 스키마, 작업 흐름이 바뀌면 관련 문서 수정도 함께 제안합니다.
- 문서가 있는 항목은 문서 기준과 구현을 맞추는 것을 우선합니다.

## 안전 규칙
- `.env`, 시크릿, 배포 설정, 운영 설정, 데이터베이스 스키마는 조용히 수정하지 않습니다.
- 파일 삭제, 대규모 치환, 데이터 삭제 같은 파괴적 변경은 먼저 알립니다.
- 되돌리기 어려운 작업은 보수적으로 처리합니다.
- 부작용이 있는 명령은 실행 전에 목적을 짧게 설명합니다.

## Claude 작업 방식
- 먼저 변경 내용을 설명한 뒤 구현합니다.
- 여러 선택지가 있으면 가장 실용적인 안을 먼저 제시합니다.
- 기존 구조와 규칙을 우선 따릅니다.
- 디버깅 시에는 수정안보다 먼저 원인 가능성을 설명합니다.
- 로그가 길면 핵심만 요약합니다.
- 위 규칙과 충돌하는 구현이 필요하면 먼저 이유를 설명하고 확인을 구합니다.
